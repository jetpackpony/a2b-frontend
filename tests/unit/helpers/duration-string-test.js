
import { durationString } from 'a2b/helpers/duration-string';
import { module, test } from 'qunit';

module('Unit | Helper | duration string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = durationString([42]);
  assert.ok(result);
});


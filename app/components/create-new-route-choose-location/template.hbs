<div class="alert alert-danger">At the moment you can only add routes in 11 countries in South-East Asia: Vietnam, Cambodia, Laos, Myanmar, Thailand, Malaysia, Brunei, East Timor, Indonesia, Singapore, Philippines</div>
<h4>{{countryHeader}}</h4>
<select {{action "countryChanged" on='change'}}>
  <option value="---" disabled selected>---</option>
  {{#each countries as |country|}}
    <option value={{country.value}}>{{country.text}}</option>
  {{/each}}
</select>

{{#if countrySet}}
  <h4>{{cityHeader}}</h4>
  {{place-autocomplete-field
      handlerController=this
      inputClass="form-control form-control"
      placeChangedCallback='cityChanged'
      types='(cities)'
      restrictions=countryRestriction
  }}

  {{#if citySet}}
    <h4>Enter the address below or point it on the map</h4>

    {{#if showAddress}}
      <a href='#' {{action "toggleAddress" on='click'}}>
        I don't know the exact address!
      </a>
      <div class="form-group">
        {{place-autocomplete-field
            value=address
            handlerController= this
            inputClass="form-control form-control"
            placeChangedCallback='addressChanged'
            types='address'
            restrictions=countryRestriction
        }}
      </div>

      {{input type="hidden" id="coords" value=coords}}
    {{else}}
      <div>
        <label class='col-form-label'>Describe the location for us</label>
        {{textarea class="form-control" value=comment}}
      </div>
      <div>
        <a href='#' {{action "toggleAddress" on='click'}}>
          Let me try to search for an address again!
        </a>
      </div>
    {{/if}}

    {{#if addressSet}}
      <button {{action "next"}}>Next</button>
    {{/if}}
  {{/if}}
{{/if}}
